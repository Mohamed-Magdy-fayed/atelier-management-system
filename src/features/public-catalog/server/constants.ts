/** IANA timezone used for public “calendar day” availability checks. */
export const PUBLIC_BUSINESS_TIMEZONE = "Africa/Cairo";

/** Scan window when suggesting the next open date on the public dress page. */
export const NEXT_AVAILABLE_SCAN_DAYS = 60;
