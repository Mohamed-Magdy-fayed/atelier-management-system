---
description: Agent workflow, verification, and lessons learned (always follow)
alwaysApply: true
---

# Agent workflow

These rules apply to every task. **Update this file** when a mistake or convention is discovered in a session so it is not repeated in later sessions.

## Verification

- After substantive app or type changes, run **`npm run build`** and fix failures before considering the task done.
- Use **npm** for package scripts (not pnpm/yarn unless the user explicitly changes tooling).
- On Windows PowerShell, chain commands with **`;`**, not `&&` (e.g. `Set-Location <repo>; npm run build`).
- Prefer fixing TypeScript/build errors over leaving the branch broken.
- Avoid exploratory shell commands unless needed for the task; prefer reading/editing source files.

## TanStack form fields (`src/components/forms/**`)

- `createFormHook` here registers **`fieldComponents` only** (`formComponents: {}`). **`useFormContext()` throws** inside `EmailField` / `PasswordField` / `FormBase` — only `useFieldContext()` is valid there.
- To show inline errors after a failed submit without `useFormContext`, mark invalid fields touched in **`useAppForm` → `onSubmitInvalid`** via `formApi.setFieldMeta` (see `hooks.tsx`). **`FormBase`** translates Zod `translationKey()` messages; do not add per-field `onChange`/`onBlur` validators or `validateField` unless the flow cannot use form-level `validators.onSubmit` (sign-in email step uses `signInEmailStepSchema` + `form.handleSubmit()` like forgot-password).

## Editing discipline

- Use normal file edits (`StrReplace`, `Write`). **Do not** run ad-hoc Node/sed/regex shell scripts to patch source files unless the user explicitly asks for a scripted migration.
- When `StrReplace` fails, re-read the file and match the exact text (including JSX closing tags).
- **JSX layout elements**: use normal HTML `div` / `span` only. This app does not use Framer Motion for shells or filters—never write `motion.div` or `</motion.div>` (invalid tags that break the build).
- Keep changes scoped to the request; avoid drive-by refactors.

## UI feedback (shadcn)

- **Inline notices** (form hints, prerequisites, non-blocking guidance): use `Alert`, `AlertTitle`, and `AlertDescription` from `@/components/ui/alert` with the correct **variant** — `default` for informational messages, `destructive` for errors/blockers. Include a lucide icon (e.g. `InfoIcon`, `AlertTriangleIcon`) as a child of `Alert`, matching existing usage in `form-alert.tsx` and `dress-availability-checker.tsx`.
- Do **not** hand-roll notice boxes with `div` + dashed borders for these cases.
- When a primary submit is **disabled**, show **why** (e.g. missing active branch on dress create) via `Alert` or field-level copy — not a silent disabled button.
- Faceted table filter popovers with long option lists: cap height and use **`ScrollArea`** inside the popover.

## System admin screens (`src/features/system/**`)

Follow [`docs/entity-blueprint.md`](../../docs/entity-blueprint.md) and [`AGENTS.md`](../../AGENTS.md).

- **Registry** (`src/features/system/registry/entities.ts`) is the source of truth for import/export, row selection, bulk actions, filters, and row actions. UI must match registry (or update registry when adding capability).
- **Page headers**: use `EntityPageHeader` with the entity `slug` from the registry (not hand-rolled `H2` / `Lead`).
- **Info dialogs**: default **audit-only** via `EntityAuditInfoDialog`; business fields stay in the table or edit form.
- **Row selection**: if the registry has `supportsRowSelection: false`, do not add `createSelectColumn` or `DataTableActionBar`. Server tables using `DataTableControlledState` still pass `rowSelection` / `onRowSelectionChange` (empty state is fine) because the type requires them.
- **Overlay forms**: modals use `OverlayFormBody` + footer `OverlayFormSubmitButton` per the blueprint Form Pattern.
- **Zod messages**: use `translationKey()` for user-facing validation, not literal English in schemas.

## Route naming (prevent conflicts)

- **`(landing-pages)` routes take priority** — public routes like `/work`, `/blog`, `/services` belong there.
- **`(system-pages)` admin routes must use a distinct path** — if the public site uses `/work`, the admin route must use `/work-mgmt`; if public uses `/blog`, admin uses `/blog-posts`.
- **Never create two `page.tsx` files that resolve to the same URL path** — Next.js will throw at build time.
- Always check `src/app/(landing-pages)/` for conflicts before adding a new system route.

## Inngest background jobs (required)

See `docs/inngest-offload-policy.md` for the full policy.

- **Never** send email, ping search engines, or make external HTTP calls inline inside a tRPC mutation.
- Fire an Inngest event instead; the function handles retries and failures outside the request lifecycle.
- Pattern: `await inngest.send({ name: “lead/submitted”, data: { leadId } })` in the mutation, then handle in `src/integrations/inngest/functions/on-lead-submitted.ts`.
- Always import the typed event map from `src/integrations/inngest/events.ts`.

## Firebase Storage (image uploads)

- All image/asset uploads use `src/integrations/firebase/storage.ts` — `uploadImage(file, path): Promise<string>`.
- No Cloudinary imports anywhere in the codebase.
- Server-side uploads use Firebase Admin SDK. Client-side uploads go through a tRPC upload mutation that calls the server helper.

## Branch-scoped operations (employees)

- **Leads, case studies, and dashboard** use `assertOperationalStaff` + `resolveListBranchId` / `assertUserCanAccessBranch` from `src/features/system/shared/staff-access.ts`.
- **Employees** must pass `branchId` from the active branch on list/mutation inputs; the server verifies branch membership. They cannot use “all branches” mode or access another branch’s rows.
- **Admins** may omit `branchId` on lists (all branches) or pass any branch; mutations still respect payload `branchId`.
- **Settings, branches admin, and employees** screens stay **admin-only** (`assertAdminRole`). Reservation **delete** / bulk delete remain admin-only.
- **Employee default landing** after sign-in: **reservations** (`/reservations`), not customers — enforced via permissions (`EMPLOYEE_BLOCKED_SCREENS` includes `dashboard`) and post-auth redirects.
- Legacy/public UX parity (receipts, multistep reservation, table pinning, theme): [`docs/legacy-parity.md`](../../docs/legacy-parity.md). Public/mobile: [`docs/public-experience.md`](../../docs/public-experience.md). Master index: [`AGENTS.md`](../../AGENTS.md).

## Documenting new lessons

When you learn something worth repeating (tooling quirk, project convention, repeated mistake):

1. Add a short bullet under the relevant section **in this file**.
2. If it belongs in product docs, update `AGENTS.md` or `docs/entity-blueprint.md` as appropriate.
3. Do not rely on chat memory alone for cross-session behavior.
